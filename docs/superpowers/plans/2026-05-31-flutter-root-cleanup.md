# Flutter Root Cleanup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the repository look and operate like a Flutter-first Zenlift project at the repository root, with React Native, Expo, Node, migration-bridge, and historical migration references removed from code, docs, skills, and project metadata.

**Architecture:** The existing Flutter app under `flutter-version/` becomes the repository root. The Flutter app keeps its feature-first Clean Architecture layout under `lib/features`, Drift/SQLite persistence under `lib/storage/drift`, SharedPreferences-backed lightweight state, and native platform folders generated by Flutter. Historical React Native/Expo source, JavaScript tooling, agent smoke tooling, migration bridge code, migration-specific docs, old OpenSpec change archives, and stale Graphify outputs are removed so the surviving project state is canonical Flutter.

**Tech Stack:** Flutter, Dart 3, go_router, flutter_riverpod, Drift, SQLite, shared_preferences, flutter_test, integration_test, flutter_lints.

---

## Current Context To Preserve

- Current repo root: `/Users/victorsanchez/Documents/portfolio/projects/zenlift`.
- Current Flutter project root: `/Users/victorsanchez/Documents/portfolio/projects/zenlift/flutter-version`.
- Existing local user work must survive:
  - Modified: `flutter-version/test/navigation/router_test.dart`
  - Untracked: `docs/superpowers/plans/2026-05-31-flutter-bottom-tab-navigation-animation.md`
- Graphify report is stale for this branch because `.graphify/GRAPH_REPORT.md` was built from commit `1d9f588`, while `git rev-parse HEAD` returned `b8023d9b343994f55b4a30c37db4d6f4737f2127`.

## File Structure After Completion

```text
.
├── AGENTS.md
├── README.md
├── DESIGN.md
├── LICENSE
├── analysis_options.yaml
├── pubspec.yaml
├── pubspec.lock
├── android/
├── assets/
│   ├── data/exercise.json
│   └── images/exercises/
├── docs/
│   ├── README.md
│   ├── ai_development_strategy.md
│   ├── architecture.md
│   ├── data_model.md
│   ├── product_context.md
│   ├── roadmap_testing.md
│   └── ux_workflows.md
├── integration_test/
├── ios/
├── lib/
├── linux/
├── macos/
├── openspec/
│   ├── config.yaml
│   └── specs/
├── test/
├── web/
└── windows/
```

Delete these root paths entirely because they belong to the old JavaScript/Expo toolchain or stale generated outputs:

```text
.expo/
.graphify/
.pnpm-store/
node_modules/
package.json
pnpm-lock.yaml
pnpm-workspace.yaml
app.json
expo-env.d.ts
tsconfig.json
jest.config.js
eslint.config.js
playwright.agent.config.ts
src/
e2e/
scripts/reset-project.js
scripts/run-maestro-ios-smoke.sh
coverage/
playwright-report/
test-results/
tmp/
```

Delete these Flutter subproject-only generated paths instead of moving them:

```text
flutter-version/.dart_tool/
flutter-version/.flutter-plugins-dependencies
flutter-version/.idea/
flutter-version/build/
flutter-version/flutter_01.log
flutter-version/flutter_02.log
```

---

### Task 1: Preflight And Dirty Worktree Protection

**Files:**
- Read: `AGENTS.md`
- Read: `docs/README.md`
- Read: `.graphify/GRAPH_REPORT.md`
- Preserve: `flutter-version/test/navigation/router_test.dart`
- Preserve: `docs/superpowers/plans/2026-05-31-flutter-bottom-tab-navigation-animation.md`

- [ ] **Step 1: Confirm the working tree before moving files**

Run:

```bash
git status --short
```

Expected output includes exactly these known local changes before implementation starts:

```text
 M flutter-version/test/navigation/router_test.dart
?? docs/superpowers/plans/2026-05-31-flutter-bottom-tab-navigation-animation.md
?? docs/superpowers/plans/2026-05-31-flutter-root-cleanup.md
```

If additional files appear, list them in the task notes and do not revert them.

- [ ] **Step 2: Preserve the modified router test diff outside the move operation**

Run:

```bash
git diff -- flutter-version/test/navigation/router_test.dart > /tmp/zenlift-router-test-before-flutter-root-cleanup.patch
```

Expected:

```text
```

Then run:

```bash
test -s /tmp/zenlift-router-test-before-flutter-root-cleanup.patch
```

Expected:

```text
```

- [ ] **Step 3: Confirm the Flutter project currently analyzes and tests before cleanup**

Run:

```bash
cd flutter-version
flutter analyze
flutter test
```

Expected:

```text
No issues found!
All tests passed!
```

If local Flutter tooling is unavailable, record the exact command failure in the implementation summary and continue with the filesystem cleanup.

- [ ] **Step 4: Confirm stale Graphify output will not be trusted**

Run:

```bash
git rev-parse HEAD
sed -n '/Built from Git commit/p' .graphify/GRAPH_REPORT.md
```

Expected:

```text
b8023d9b343994f55b4a30c37db4d6f4737f2127
- Built from Git commit: `1d9f588`
```

Because the hashes differ, delete `.graphify/` in Task 2 and rebuild only if `graphify` is available after the Flutter root move.

---

### Task 2: Remove React Native, Expo, Node, And Stale Generated Surfaces

**Files:**
- Delete: `src/`
- Delete: `e2e/`
- Delete: `scripts/reset-project.js`
- Delete: `scripts/run-maestro-ios-smoke.sh`
- Delete: `package.json`
- Delete: `pnpm-lock.yaml`
- Delete: `pnpm-workspace.yaml`
- Delete: `app.json`
- Delete: `expo-env.d.ts`
- Delete: `tsconfig.json`
- Delete: `jest.config.js`
- Delete: `eslint.config.js`
- Delete: `playwright.agent.config.ts`
- Delete: `.expo/`
- Delete: `node_modules/`
- Delete: `.pnpm-store/`
- Delete: `coverage/`
- Delete: `playwright-report/`
- Delete: `test-results/`
- Delete: `.graphify/`
- Delete: old root `android/`, `ios/`, `assets/`, and `build/` before moving Flutter equivalents

- [ ] **Step 1: Remove tracked JavaScript and Expo files**

Run:

```bash
git rm -r --ignore-unmatch \
  src \
  e2e \
  scripts/reset-project.js \
  scripts/run-maestro-ios-smoke.sh \
  package.json \
  pnpm-lock.yaml \
  pnpm-workspace.yaml \
  app.json \
  expo-env.d.ts \
  tsconfig.json \
  jest.config.js \
  eslint.config.js \
  playwright.agent.config.ts
```

Expected:

```text
rm '...'
```

The exact `rm` lines vary by tracked file count. A non-zero exit is not expected because `--ignore-unmatch` is present.

- [ ] **Step 2: Remove tracked root assets and native folders that collide with Flutter**

Run:

```bash
git rm -r --ignore-unmatch android ios assets build
```

Expected:

```text
rm '...'
```

The command can print no lines for ignored-only folders. That is acceptable.

- [ ] **Step 3: Remove ignored/generated JavaScript and stale project artifacts**

Run:

```bash
rm -rf \
  .expo \
  .graphify \
  .pnpm-store \
  node_modules \
  android \
  ios \
  assets \
  build \
  coverage \
  playwright-report \
  test-results \
  tmp
```

Expected:

```text
```

- [ ] **Step 4: Verify no old root collisions remain**

Run:

```bash
for path in android ios assets lib test integration_test web linux macos windows pubspec.yaml analysis_options.yaml; do
  if [ -e "$path" ]; then
    printf '%s still exists before Flutter move\n' "$path"
  fi
done
```

Expected:

```text
```

---

### Task 3: Move The Flutter Project To Repository Root

**Files:**
- Move: `flutter-version/lib/` to `lib/`
- Move: `flutter-version/test/` to `test/`
- Move: `flutter-version/integration_test/` to `integration_test/`
- Move: `flutter-version/android/` to `android/`
- Move: `flutter-version/ios/` to `ios/`
- Move: `flutter-version/assets/` to `assets/`
- Move: `flutter-version/linux/` to `linux/`
- Move: `flutter-version/macos/` to `macos/`
- Move: `flutter-version/web/` to `web/`
- Move: `flutter-version/windows/` to `windows/`
- Move: `flutter-version/pubspec.yaml` to `pubspec.yaml`
- Move: `flutter-version/pubspec.lock` to `pubspec.lock`
- Move: `flutter-version/analysis_options.yaml` to `analysis_options.yaml`
- Move: `flutter-version/.metadata` to `.metadata`
- Move: `flutter-version/zenlift.iml` to `zenlift.iml`
- Replace: root `.gitignore`
- Replace: root `README.md`

- [ ] **Step 1: Remove root README and gitignore so Flutter versions can replace them cleanly**

Run:

```bash
git rm --ignore-unmatch README.md .gitignore
```

Expected:

```text
rm 'README.md'
rm '.gitignore'
```

- [ ] **Step 2: Move tracked Flutter files and directories**

Run:

```bash
git mv flutter-version/lib lib
git mv flutter-version/test test
git mv flutter-version/integration_test integration_test
git mv flutter-version/android android
git mv flutter-version/ios ios
git mv flutter-version/assets assets
git mv flutter-version/linux linux
git mv flutter-version/macos macos
git mv flutter-version/web web
git mv flutter-version/windows windows
git mv flutter-version/pubspec.yaml pubspec.yaml
git mv flutter-version/pubspec.lock pubspec.lock
git mv flutter-version/analysis_options.yaml analysis_options.yaml
git mv flutter-version/.metadata .metadata
git mv flutter-version/.gitignore .gitignore
git mv flutter-version/README.md README.md
git mv flutter-version/zenlift.iml zenlift.iml
```

Expected:

```text
```

- [ ] **Step 3: Remove Flutter subproject generated artifacts**

Run:

```bash
rm -rf \
  flutter-version/.dart_tool \
  flutter-version/.flutter-plugins-dependencies \
  flutter-version/.idea \
  flutter-version/build \
  flutter-version/flutter_01.log \
  flutter-version/flutter_02.log
```

Expected:

```text
```

- [ ] **Step 4: Delete the now-empty Flutter subfolder**

Run:

```bash
rmdir flutter-version
```

Expected:

```text
```

If `rmdir` reports that the folder is not empty, run this to inspect the remaining files:

```bash
find flutter-version -maxdepth 2 -print | sort
```

Only generated files listed in Task 2 or Task 3 may be removed with `rm -rf`.

- [ ] **Step 5: Verify the user’s router test edit moved to the root test tree**

Run:

```bash
git diff -- test/navigation/router_test.dart
```

Expected output includes:

```diff
+  testWidgets('bottom navigation tab switches do not overlap route pages', (
+    tester,
+  ) async {
+    await tester.pumpZenliftRoute();
+
+    await tester.tap(find.byKey(ZenliftRouteKeys.historyTab));
+    await tester.pump();
+    await tester.pump(const Duration(milliseconds: 100));
+
+    expect(find.byKey(ZenliftRouteKeys.historyScreen), findsOneWidget);
+    expect(find.byKey(ZenliftRouteKeys.homeScreen), findsNothing);
+    expect(selectedTabIndex(tester), 3);
+  });
```

---

### Task 4: Remove Flutter-Cutover Bridge Code And Source-Stack References

**Files:**
- Delete: `lib/storage/migration_bridge/first_launch_migrator.dart`
- Delete: `lib/storage/migration_bridge/migration_bridge_verifier.dart`
- Delete: `lib/storage/migration_bridge/shared_preferences_migration_bridge_settings.dart`
- Delete: `test/storage/migration_bridge/first_launch_migrator_test.dart`
- Delete: `test/storage/migration_bridge/migration_bridge_verifier_test.dart`
- Delete: `test/storage/migration_bridge/shared_preferences_migration_bridge_settings_test.dart`
- Modify: `pubspec.yaml`
- Modify: `test/widgets/zenlift_golden_test.dart`
- Modify: `test/storage/seed/exercise_seed_data_test.dart`
- Keep: Drift database `MigrationStrategy` and `_migrations` table because those are normal database schema mechanics, not evidence of a framework change.

- [ ] **Step 1: Delete bridge-only code and tests**

Run:

```bash
git rm -r --ignore-unmatch lib/storage/migration_bridge test/storage/migration_bridge
```

Expected:

```text
rm 'lib/storage/migration_bridge/first_launch_migrator.dart'
rm 'lib/storage/migration_bridge/migration_bridge_verifier.dart'
rm 'lib/storage/migration_bridge/shared_preferences_migration_bridge_settings.dart'
rm 'test/storage/migration_bridge/first_launch_migrator_test.dart'
rm 'test/storage/migration_bridge/migration_bridge_verifier_test.dart'
rm 'test/storage/migration_bridge/shared_preferences_migration_bridge_settings_test.dart'
```

- [ ] **Step 2: Change the package description**

Edit `pubspec.yaml` so the first lines are exactly:

```yaml
name: zenlift
description: "Zenlift local-first workout tracker."
publish_to: 'none'
version: 1.0.0+1
```

- [ ] **Step 3: Remove source-stack language from the golden test comment**

In `test/widgets/zenlift_golden_test.dart`, replace this comment:

```dart
// DESIGN.md specifies Inter and JetBrains Mono, but the Flutter migration
// uses Flutter's default test fonts in CI.
```

with:

```dart
// DESIGN.md specifies Inter and JetBrains Mono; widget tests use Flutter's
// default test fonts in CI.
```

- [ ] **Step 4: Remove source-stack language from the seed test name**

In `test/storage/seed/exercise_seed_data_test.dart`, replace:

```dart
test('generateSeedId is deterministic and matches Expo relationship IDs', () {
```

with:

```dart
test('generateSeedId is deterministic and matches seeded relationship IDs', () {
```

- [ ] **Step 5: Verify no Flutter cutover bridge references remain in app source**

Run:

```bash
rg -n "flutter-version|Flutter migration|React Native|Expo source|Expo relationship|cutover|migration_bridge|migration bridge" lib test integration_test pubspec.yaml README.md
```

Expected:

```text
```

---

### Task 5: Rewrite README, AGENTS, And Compact Docs As Flutter-First

**Files:**
- Replace: `README.md`
- Replace: `AGENTS.md`
- Modify: `docs/README.md`
- Modify: `docs/product_context.md`
- Modify: `docs/architecture.md`
- Modify: `docs/data_model.md`
- Modify: `docs/roadmap_testing.md`
- Modify: `docs/ai_development_strategy.md`
- Modify: `docs/ux_workflows.md`
- Delete: `docs/flutter_migration/`

- [ ] **Step 1: Replace `README.md` with Flutter-first project copy**

Use this complete content for `README.md`:

```markdown
# Zenlift

Zenlift is a mobile-first workout tracker for people who train in the gym and want a fast, reliable way to plan routines, log sets, and understand progress over time.

The product is intentionally focused on one loop:

```text
Create a routine -> Start a workout -> Log sets -> Finish the session -> Review progress
```

It is not a coach dashboard, gym management system, social network, marketplace, or nutrition platform. The goal is a personal training log that feels quick enough to use mid-set and dependable enough to trust with months of workout history.

## What Zenlift Does

- Create routines, training days, and exercise plans.
- Start workouts from a saved routine.
- Log weight, reps, and completed sets quickly.
- Recover active workouts if the app is closed.
- Review workout history and exercise progress.
- Track local data first, without requiring an account or network connection.

## Product Principles

- Active Workout is the most important screen.
- A completed set should be loggable in under 3 seconds.
- Completed workout data should never be lost.
- Core flows must work offline.
- Routine edits must not mutate past workout sessions.
- The interface is dark-first, focused, and designed for one-handed gym use.

## Tech Stack

- Flutter and Dart 3.
- go_router for route configuration.
- flutter_riverpod for app state where shared state is needed.
- Drift with SQLite for structured workout data.
- shared_preferences for lightweight persisted state and settings.
- fl_chart for progress visualization.
- file_picker and share_plus for local data portability.
- flutter_test and integration_test for unit, widget, repository, and core-loop verification.

## Project Structure

```text
lib/
  app/                    App bootstrap, router, and route composition
  core/                   Shared date, ID, and small utility primitives
  features/               Feature-first application, domain, data, and presentation code
  storage/                Drift database, connection, schema, and seed data
  theme/                  Design tokens and Flutter theme helpers
  widgets/                Shared reusable UI widgets

test/                     Unit, widget, controller, repository, and theme tests
integration_test/         End-to-end Flutter core-loop tests
assets/                   Exercise data and exercise images
docs/                     Product, architecture, data, testing, and AI workflow notes
```

Screens stay thin. Business rules, PR detection, volume calculations, unit conversion, and persistence logic live outside route widgets so they can be tested without rendering full app flows.

## Getting Started

### Requirements

- Flutter stable with Dart 3.
- Xcode for iOS builds on macOS.
- Android Studio or Android command-line tools for Android builds.

### Install

```bash
flutter pub get
```

### Run The App

```bash
flutter run
```

For a specific target:

```bash
flutter devices
flutter run -d <device-id>
```

## Quality Checks

Run the core local checks:

```bash
flutter analyze
flutter test
flutter test integration_test/core_loop_test.dart
```

The most important areas to keep covered are:

- Workout volume and 1RM calculations.
- Personal record detection.
- Unit conversion.
- Drift repositories and schema behavior.
- Active workout persistence and recovery.
- Core-loop navigation from routine creation through workout completion.

## Local-First Data

Zenlift stores core workout data on-device. Drift and SQLite are used for routines, exercises, sessions, sets, settings snapshots, and history. shared_preferences is reserved for small, high-frequency state such as settings and active workout recovery helpers.

This keeps the app simple, fast, and usable in the gym even with poor connectivity.

## Design Direction

Zenlift uses a dark-first visual system built around monochromatic depth, high-contrast data, and restrained purple/lavender accents. Green is reserved for success and completed states, not primary actions.

See [DESIGN.md](DESIGN.md) for the full color, typography, spacing, and component system.

## Documentation

Start with [docs/README.md](docs/README.md). It points to compact docs for product scope, UX flows, architecture, data modeling, roadmap, testing, and AI development workflow.

Useful entry points:

- [Product context](docs/product_context.md)
- [UX workflows](docs/ux_workflows.md)
- [Architecture](docs/architecture.md)
- [Data model](docs/data_model.md)
- [Roadmap and testing](docs/roadmap_testing.md)

## Development Notes

- Keep the core workout loop small and fast.
- Prefer local-first behavior until a backend is explicitly required.
- Put domain calculations in pure Dart under feature domain layers.
- Keep SQLite access inside Drift repositories.
- Use UUID text IDs for records.
- Add focused tests when touching calculations, repositories, schema behavior, or active-session recovery.

## License

Apache-2.0. See [LICENSE](LICENSE).
```

- [ ] **Step 2: Replace `AGENTS.md` with Flutter-first instructions**

Use this complete content for `AGENTS.md`:

```markdown
# Zenlift Agent Instructions

Zenlift is a B2C mobile-first workout tracker for gym users. Keep the product focused on the core loop:

```text
Crear rutina -> Iniciar workout -> Registrar sets -> Finalizar sesión -> Ver progreso
```

Do not turn Zenlift into a CRM, SaaS, coach dashboard, web admin, marketplace, booking system, nutrition platform, or social-first product.

## Documentation Loading

To reduce tokens, do not open the full blueprint by default.

1. Start with `docs/README.md`.
2. Open only the compact doc relevant to the task:
   - Product/scope/positioning: `docs/product_context.md`
   - UX/screens/flows/copy: `docs/ux_workflows.md`
   - Architecture/state/storage/performance/errors: `docs/architecture.md`
   - SQLite/entities/repositories/seeds: `docs/data_model.md`
   - MVP/roadmap/testing/release/risks: `docs/roadmap_testing.md`
   - AI task planning/delegation: `docs/ai_development_strategy.md`
3. Open `docs/zenlift_product_blueprint.md` only when the compact docs are insufficient, ambiguous, or you are updating strategic documentation.

## Context7

Use Context7 MCP to fetch current documentation whenever the user asks about a library, framework, SDK, API, CLI tool, or cloud service. This includes Flutter, Dart, go_router, Riverpod, Drift, SQLite, shared_preferences, fl_chart, file_picker, share_plus, Android Gradle, Xcode, and similar tools.

Always start with `resolve-library-id` unless the user provides an exact `/org/project` library ID, then call `query-docs` with the selected library and the full question.

Do not use Context7 for general refactors, business logic debugging, code review, or general programming concepts.

## Flutter

Before writing Flutter or Dart code that depends on framework, package, CLI, platform, or build-system behavior, verify the current project package versions in `pubspec.yaml` and fetch current docs with Context7.

Use `flutter analyze` and focused `flutter test` commands for verification. Use `flutter test integration_test/core_loop_test.dart` when touching navigation, persistence, or the core workout loop.

## Product Rules

- Optimize Active Workout above all other screens.
- A set should be loggable in under 3 seconds.
- Never risk losing workout data; autosave completed sets.
- The app must work offline for core flows.
- Use UUID text IDs from day 1.
- Use dark theme by default. Reference `DESIGN.md` for the complete color system, typography, spacing, and component tokens. Do not use green as primary; reserve green for success/completed states.
- Editing routines must not mutate past workout sessions.
- Prefer local-first data and simple architecture until a backend is explicitly required.

## Implementation Rules

- Keep domain calculations in pure Dart under feature domain layers.
- Keep route widgets and screens thin; avoid heavy business logic in route files.
- Use Drift/SQLite for structured workout data and shared_preferences for lightweight settings/state.
- Keep repositories behind feature-level domain contracts.
- Avoid UI kits that add unnecessary weight.
- Add tests for calculations, PR detection, unit conversion, repositories, schema changes, and active-session recovery when touching those areas.

## Testing

- Use `flutter analyze` for static analysis.
- Use `flutter test` for unit, widget, repository, controller, and theme tests.
- Use `flutter test integration_test/core_loop_test.dart` for the core loop when navigation, storage, or workout completion changes.
- Still test on Android hardware for keyboard ergonomics, haptics, offline behavior, performance, and active-session recovery.
```

- [ ] **Step 3: Delete migration-specific docs**

Run:

```bash
git rm -r --ignore-unmatch docs/flutter_migration
```

Expected:

```text
rm 'docs/flutter_migration/README.md'
rm 'docs/flutter_migration/design_parity.md'
rm 'docs/flutter_migration/spec_matrix.md'
rm 'docs/flutter_migration/testing.md'
```

- [ ] **Step 4: Update compact docs to Flutter wording**

Apply these exact content changes:

```text
docs/README.md
- Remove the "Continuar la migración Flutter..." row.
- Keep the Context7 rule and change examples to Flutter, Dart, go_router, Riverpod, Drift, SQLite, shared_preferences, fl_chart, file_picker, share_plus, Android Gradle, and Xcode.

docs/product_context.md
- Replace "Producto mobile-first construido con React Native + Expo." with "Producto mobile-first construido con Flutter y Dart."

docs/architecture.md
- Replace the stack section with Flutter, Dart 3, go_router, flutter_riverpod, Drift/SQLite, shared_preferences, Flutter ThemeData, fl_chart, file_picker, and share_plus.
- Replace the suggested structure with lib/app, lib/core, lib/features, lib/storage, lib/theme, and lib/widgets.
- Replace MMKV retry-queue wording with shared_preferences-backed lightweight recovery state.

docs/data_model.md
- Delete the "Flutter migration notes" section.
- Keep UUID, offline-first, Drift/SQLite, app_settings, and _migrations content as current architectural facts.

docs/roadmap_testing.md
- Replace the "Flutter migration testing" section with "Flutter testing".
- Replace JavaScript-agent smoke commands with Flutter commands: flutter analyze, flutter test, flutter test integration_test/core_loop_test.dart.
- Remove Playwright, Expo web, pnpm, Jest, and Maestro as canonical project checks.

docs/ai_development_strategy.md
- Replace agent-browser/Expo guidance with Flutter verification guidance.
- Replace TypeScript/React component strengths with Dart, Flutter widgets, Drift repositories, route composition, and widget tests.

docs/ux_workflows.md
- Keep product flows.
- Remove rest-timer as a guaranteed current component if the app has no active timer implementation.
```

- [ ] **Step 5: Update the untracked Flutter navigation plan to root paths**

Edit `docs/superpowers/plans/2026-05-31-flutter-bottom-tab-navigation-animation.md`:

```text
Replace every `flutter-version/test/navigation/router_test.dart` with `test/navigation/router_test.dart`.
Replace every `flutter-version/lib/app/router.dart` with `lib/app/router.dart`.
Replace every `flutter-version/lib/features/home/presentation/zenlift_tab_shell.dart` with `lib/features/home/presentation/zenlift_tab_shell.dart`.
Replace every `cd flutter-version && flutter test ...` command with `flutter test ...`.
```

- [ ] **Step 6: Verify docs do not describe a framework change**

Run:

```bash
rg -n "flutter-version|Flutter migration|React Native|Expo|expo|pnpm|Node\\.js|Jest|Playwright|Maestro|Zustand|MMKV|FlashList|migraci[oó]n Flutter|cutover|source app baseline" README.md AGENTS.md docs
```

Expected:

```text
```

---

### Task 6: Clean Skills, OpenSpec, And Historical Planning Artifacts

**Files:**
- Delete: `skills-lock.json`
- Delete: React/Expo/Node specific folders under `.agents/skills/`
- Create: `.agents/skills/zenlift-flutter/SKILL.md`
- Create: `.codex/skills/zenlift-flutter/SKILL.md`
- Delete: `docs/superpowers/plans/2026-05-30-accessible-exercise-filters.md`
- Delete: `docs/superpowers/plans/2026-05-30-active-workout-audit-openspec-plan.md`
- Delete: `docs/superpowers/plans/2026-05-30-exercise-visual-tab.md`
- Delete: `docs/superpowers/plans/2026-05-30-flutter-migration.md`
- Delete: `docs/superpowers/plans/2026-05-30-i18n-es-en.md`
- Delete: `docs/superpowers/plans/2026-05-30-remove-active-exercise-collapse.md`
- Delete: `docs/superpowers/plans/2026-05-30-routines-screen-bottom-navigation.md`
- Delete: `openspec/changes/archive/`
- Delete: `openspec/changes/migrate-app-to-flutter/`
- Delete: active OpenSpec changes that only target deleted React Native/Expo code
- Modify: `openspec/specs/`

- [ ] **Step 1: Remove JavaScript, React, Expo, and Node skills from project-local agent skills**

Run:

```bash
git rm -r --ignore-unmatch \
  skills-lock.json \
  .agents/skills/building-native-ui \
  .agents/skills/composition-patterns \
  .agents/skills/creating-reanimated-animations \
  .agents/skills/expo-api-routes \
  .agents/skills/expo-cicd-workflows \
  .agents/skills/expo-deployment \
  .agents/skills/expo-dev-client \
  .agents/skills/expo-tailwind-setup \
  .agents/skills/native-data-fetching \
  .agents/skills/nodejs-backend-patterns \
  .agents/skills/nodejs-best-practices \
  .agents/skills/react-best-practices \
  .agents/skills/react-hook-form \
  .agents/skills/typescript-advanced-types \
  .agents/skills/upgrading-expo \
  .agents/skills/use-dom \
  .agents/skills/zod
```

Expected:

```text
rm '...'
```

- [ ] **Step 2: Add a Flutter-specific project skill for agents**

Create `.agents/skills/zenlift-flutter/SKILL.md` and `.codex/skills/zenlift-flutter/SKILL.md` with this exact content:

```markdown
---
name: zenlift-flutter
description: Use when implementing, testing, or reviewing Zenlift Flutter code.
---

# Zenlift Flutter

Use this skill for code changes in the Zenlift Flutter app.

## Context

- Read `AGENTS.md`.
- Read `docs/README.md`.
- Open only the compact doc that matches the task.
- Use Context7 for package, SDK, CLI, or platform docs before writing package-specific code.

## Architecture

- Keep route widgets thin.
- Put business rules in feature domain/application layers.
- Keep Drift access in data repositories.
- Keep reusable visual primitives in `lib/widgets` or `lib/theme`.
- Preserve local-first behavior and offline core flows.

## Verification

Run focused checks first, then broaden:

```bash
flutter analyze
flutter test
flutter test integration_test/core_loop_test.dart
```

If a command cannot run because local tooling or a simulator is unavailable, report the exact command and error.
```

- [ ] **Step 3: Delete old implementation plans that reference removed source paths or old stack**

Run:

```bash
git rm --ignore-unmatch \
  docs/superpowers/plans/2026-05-30-accessible-exercise-filters.md \
  docs/superpowers/plans/2026-05-30-active-workout-audit-openspec-plan.md \
  docs/superpowers/plans/2026-05-30-exercise-visual-tab.md \
  docs/superpowers/plans/2026-05-30-flutter-migration.md \
  docs/superpowers/plans/2026-05-30-i18n-es-en.md \
  docs/superpowers/plans/2026-05-30-remove-active-exercise-collapse.md \
  docs/superpowers/plans/2026-05-30-routines-screen-bottom-navigation.md
```

Expected:

```text
rm 'docs/superpowers/plans/2026-05-30-accessible-exercise-filters.md'
rm 'docs/superpowers/plans/2026-05-30-active-workout-audit-openspec-plan.md'
rm 'docs/superpowers/plans/2026-05-30-exercise-visual-tab.md'
rm 'docs/superpowers/plans/2026-05-30-flutter-migration.md'
rm 'docs/superpowers/plans/2026-05-30-i18n-es-en.md'
rm 'docs/superpowers/plans/2026-05-30-remove-active-exercise-collapse.md'
rm 'docs/superpowers/plans/2026-05-30-routines-screen-bottom-navigation.md'
```

- [ ] **Step 4: Delete old OpenSpec change history and obsolete active changes**

Run:

```bash
git rm -r --ignore-unmatch \
  openspec/changes/archive \
  openspec/changes/migrate-app-to-flutter \
  openspec/changes/accessible-exercise-filters \
  openspec/changes/exercise-visual-tab \
  openspec/changes/i18n-es-en \
  openspec/changes/remove-active-exercise-collapse \
  openspec/changes/routines-screen-bottom-navigation \
  openspec/changes/stabilize-active-workout-set-logging
```

Expected:

```text
rm '...'
```

- [ ] **Step 5: Update OpenSpec specs to remove old stack references**

Run:

```bash
rg -l "Expo|expo|React Native|react-native|TypeScript|Zustand|MMKV|FlashList|Jest|Playwright|Maestro|pnpm|flutter-version|Flutter migration|migraci[oó]n Flutter|source app baseline" openspec/specs
```

For each listed `openspec/specs/**/spec.md` file, update the technology references to Flutter equivalents:

```text
Expo Router -> go_router
React Native component/screen -> Flutter widget/screen
TypeScript -> Dart
Zustand/MMKV -> Riverpod/shared_preferences where shared state or lightweight persistence is needed
expo-sqlite -> Drift/SQLite
Jest -> flutter_test
Playwright/Expo web/Maestro agent smoke -> Flutter integration test plus manual device testing
pnpm commands -> flutter commands
flutter-version/ paths -> root paths
```

Keep behavioral requirements intact: local-first storage, UUID IDs, active workout recovery, set logging under 3 seconds, dark theme, and routine edits not mutating historical sessions.

- [ ] **Step 6: Verify skills and OpenSpec are clean**

Run:

```bash
rg -n "Expo|expo|React Native|react-native|Node\\.js|node_modules|TypeScript|Zustand|MMKV|FlashList|Jest|Playwright|Maestro|pnpm|flutter-version|Flutter migration|migraci[oó]n Flutter|cutover" .agents .codex openspec docs/superpowers/plans
```

Expected output may include this current implementation plan only:

```text
docs/superpowers/plans/2026-05-31-flutter-root-cleanup.md:...
```

Before the final commit for the cleanup branch, delete or move `docs/superpowers/plans/2026-05-31-flutter-root-cleanup.md` if the branch must have zero historical cleanup references in committed files.

---

### Task 7: Regenerate Flutter Metadata And Verify The Root Project

**Files:**
- Modify generated if needed: `pubspec.lock`
- Modify generated if needed: `.metadata`
- Modify generated if needed: `lib/storage/drift/app_database.g.dart`
- Modify generated if needed: platform plugin registrants under `android/`, `ios/`, `linux/`, `macos/`, `web/`, or `windows/`

- [ ] **Step 1: Install Flutter dependencies at the root**

Run:

```bash
flutter pub get
```

Expected includes:

```text
Got dependencies!
```

- [ ] **Step 2: Regenerate Drift code if package resolution or schema generation needs it**

Run:

```bash
dart run build_runner build --delete-conflicting-outputs
```

Expected includes:

```text
Built with build_runner
```

If it exits with "0 outputs" or equivalent no-op wording, that is acceptable.

- [ ] **Step 3: Run static analysis**

Run:

```bash
flutter analyze
```

Expected:

```text
No issues found!
```

- [ ] **Step 4: Run the full Flutter test suite**

Run:

```bash
flutter test
```

Expected:

```text
All tests passed!
```

- [ ] **Step 5: Run the focused router regression test that had local user edits**

Run:

```bash
flutter test test/navigation/router_test.dart --plain-name "bottom navigation tab switches do not overlap route pages"
```

Expected:

```text
All tests passed!
```

- [ ] **Step 6: Run the core-loop integration test**

Run:

```bash
flutter test integration_test/core_loop_test.dart
```

Expected:

```text
All tests passed!
```

If no simulator/device is available and the command fails for environment reasons, capture the exact error and leave this check explicitly unverified.

- [ ] **Step 7: Run the final no-trace scan**

Run:

```bash
rg -n "flutter-version|Flutter migration|migraci[oó]n Flutter|source app baseline|React Native|react-native|Expo|expo|Node\\.js|node_modules|pnpm|Zustand|MMKV|FlashList|Jest|Playwright|Maestro|migration_bridge|cutover" . -g '!docs/superpowers/plans/2026-05-31-flutter-root-cleanup.md' -g '!build/**' -g '!.dart_tool/**' -g '!.git/**'
```

Expected:

```text
```

- [ ] **Step 8: Confirm the final project tree is Flutter-rooted**

Run:

```bash
test -f pubspec.yaml
test -d lib
test -d test
test -d integration_test
test -d android
test -d ios
test ! -d flutter-version
test ! -d src
test ! -f package.json
test ! -d node_modules
```

Expected:

```text
```

- [ ] **Step 9: Review final git status**

Run:

```bash
git status --short
```

Expected:

```text
R  flutter-version/lib/... -> lib/...
R  flutter-version/test/... -> test/...
R  flutter-version/integration_test/... -> integration_test/...
R  flutter-version/android/... -> android/...
R  flutter-version/ios/... -> ios/...
D  src/...
D  package.json
D  pnpm-lock.yaml
M  README.md
M  AGENTS.md
M  docs/...
```

The exact status will be longer. Confirm there are no unexpected edits outside the cleanup scope and that the user’s router-test regression remains present at `test/navigation/router_test.dart`.

---

## Self-Review

- Spec coverage: The plan moves Flutter from `flutter-version/` to root, removes React Native/Expo/Node files and caches, rewrites README/AGENTS/docs/skills/OpenSpec around Flutter, removes migration-bridge code and docs, preserves current local user edits, and verifies that no old-stack or framework-change references remain.
- Placeholder scan: The plan uses concrete paths, commands, expected outputs, and exact replacement text for the main public docs and project skill. There are no empty implementation slots.
- Type consistency: Commands consistently use root Flutter paths after Task 3. Verification commands use `flutter analyze`, `flutter test`, and `flutter test integration_test/core_loop_test.dart` from the repository root.
